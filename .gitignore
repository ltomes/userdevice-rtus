# Training data — NEVER committed. Regenerate with scripts/gen_pairs_h264.sh
# or userdevice_rtus.tools.gen_pairs_v2. See data/README.md.
datasets/
data/**
!data/README.md
!data/*.example.yml

# Benchmark/test sets — fetched, not stored. scripts/fetch_testsets.sh
testsets/

# Training outputs: checkpoints, states, logs, visual A/B dumps
experiments/
onnx_out/
pretrained/
results/
preview/
visual_ab*/
*.safetensors
*.pth
*.onnx
*.plan
*.npz
*.ckpt

# Local machine config (paths, mounts, device ids)
*.local.yml
.env

__pycache__/
*.pyc
